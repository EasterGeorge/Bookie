<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="easter.george.bookie">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BkClubsActivity" android:theme="@style/SplashTheme"></activity>
        <activity android:name=".GenresActivity" />
        <activity android:name=".ChatActivity" />
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MyBookClubsActivity"
            android:label="@string/title_activity_my_book_clubs" />
        <activity
            android:name=".CreateBookClubActivity"
            android:theme="@style/SplashTheme" />
        <activity
            android:name=".SuggestedBookClubsActivity"
            android:theme="@style/SplashTheme" />
        <activity
            android:name=".ChooseBkGenreActivity"
            android:theme="@style/SplashTheme" />
        <activity
            android:name=".SignUpActivity"
            android:theme="@style/SplashTheme" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/SplashTheme" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/SplashTheme" />
    </application>

</manifest>